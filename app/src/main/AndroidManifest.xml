<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mz.chess">

    <application
        android:name=".ChessApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".menu.PreferencesActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".menu.PrivacyPolicyActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".menu.MoreGamesActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".game.Game"
            android:screenOrientation="portrait"/>

        <activity android:name=".menu.MainMenu"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu.NewGameActivity"
            android:screenOrientation="portrait"/>
    </application>
</manifest>
